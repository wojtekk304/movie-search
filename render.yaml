services:
  - type: web
    name: movie-app
    env: java
    plan: free
    buildCommand: ./mvnw clean package
    startCommand: java -cp target/classes com.movie.App
    envVars:
      - key: JAVA_VERSION
        value: 21
