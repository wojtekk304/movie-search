services:
  - type: web
    name: movie-search
    runtime: java
    buildCommand: mvn clean package
    startCommand: java -cp target/classes com.movie.App
    plan: free
    envVars:
      - key: JAVA_VERSION
        value: 21
